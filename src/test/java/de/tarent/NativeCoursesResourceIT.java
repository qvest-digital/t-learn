package de.tarent;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCoursesResourceIT extends CoursesResourceTest {

    // Execute the same tests but in native mode.
}